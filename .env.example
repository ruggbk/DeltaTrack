# Optional. fetch_bills.py downloads from keyless govinfo bulk data by default;
# a Congress.gov API key is only needed for `--source api` or `download-all`
# year-range discovery. Get a free key at https://api.congress.gov/sign-up/
CONGRESS_API_KEY=your_key_here
